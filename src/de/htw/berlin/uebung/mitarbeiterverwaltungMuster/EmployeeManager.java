package de.htw.berlin.uebung.mitarbeiterverwaltungMuster;

public class EmployeeManager {
    /*private List<Employee> employees = new ArrayList<>();

    public void addEmployee(int id, String name, int yearJoined) {
        employees.add(new Employee(id, name, yearJoined));
    }

    public void removeEmployee(int id) {
        employees.removeIf(e -> e.getId() == id);
    }

    public void findEmployeeById(int id) {
        employees.stream()
                .filter(e -> e.getId() == id)
                .findFirst()
                .ifPresent(System.out::println);
    }

    public void printAllEmployees() {
        employees.forEach(System.out::println);
    }

    public void sortEmployeesByYearJoined() {
        employees.sort(Comparator.comparingInt(Employee::getYearJoined));
        printAllEmployees();
    }*/
}
